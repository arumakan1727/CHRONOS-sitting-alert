version: '3'

# Taskfile のドキュメント: https://taskfile.dev/usage/

tasks:
  default:
    cmds:
      - task: list-tasks

  list-tasks:
    desc: タスクランナー `task` のタスクを一覧表示する。
    cmd: task --list
    silent: true

  #--------------------------------------------
  # lint, format, typecheck

  fmt:
    desc: ソースコードを整形する。
    cmd: find . -type f -name '*.hpp' -o -name '*.cpp' -o -name '*.h' | xargs clang-format -i
